import { ReservationVersionConflictPreview } from "./reservation-version-conflict-preview";

export const metadata = {
  title: "Reservation Version Conflict Preview",
};

export default function ReservationVersionConflictPage() {
  return <ReservationVersionConflictPreview />;
}
