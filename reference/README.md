# Finalized Platform Compatibility Reference

`finalized-platform/` is a tracked-file export of the finalized hotel platform at:

- Commit: `dbce321f61144b50a94bd11a068fa5897b0f2293`
- Git tree: `1568e8941f95c8d5962af1e097a96e2dfdde7031`
- Exported tracked files: 406

It exists only to support interface discovery, contract comparison, integration planning, and non-mutating compatibility checks for the AI OS add-on.

The export excludes source Git metadata and anything not tracked by that commit, including `.env.local`, `node_modules`, `.next`, `.open-next`, `.wrangler`, Playwright reports, test results, and the source workspace's untracked `SUT Logo.png`.

Do not modify this snapshot. If the finalized platform changes after live deployment, add a separately reviewed baseline update rather than editing files in place.
