# Current Focus

## Current planning gate

`SUT-AIOS-GOV-043` prepares derived portability and resource-governance
planning only. It creates no provider, account, domain, runtime, database,
credential, deployment, or production behavior.

## Next valid product dependency

`SUT-AIOS-P2-004` — the static intervention-proposal contract — remains the
next existing product dependency. `P2-005`, `P2-006`, and `P2-007` are new
static infrastructure authorities and must be completed before any Phase 3
runtime implementation. Their exact validator admissions remain separate
governance prerequisites at activation.

## Guardrails

- Do not modify `reference/finalized-platform/**` or `docs/architecture/source/**`.
- Do not deploy, access production systems, change credentials, or enable
  autonomous operation.
- Keep all provider/worker capabilities Tier 0/shadow.
- Guest/public, Staff/control, and AI/workload boundaries must remain separate.
- Use the active packet, isolated worktree, independent verification, and
  durable evidence workflow.
